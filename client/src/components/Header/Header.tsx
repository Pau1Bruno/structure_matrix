import React from "react";
import styles from "./Header.module.scss";

const Header = () => {
  return (
    <div className={styles.header}>
      <h1>Выделение структурных особенностей</h1>
    </div>
  );
};

export default Header;
